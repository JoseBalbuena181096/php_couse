<?php include 'includes/header.php';

// Tipos de datos en PHP

// BOOLEAN
$logueado = true;
var_dump($logueado);

// Entero
$numero = 200;
var_dump($numero);

// Floats
$float = 200.5;
var_dump($float);

// string
$nombre  = "jose";
var_dump($nombre);

// Arreglos
$array = [];
var_dump($array);

include 'includes/footer.php';
